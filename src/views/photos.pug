extends design.pug

block head
  link(rel="stylesheet", href="/photos.css")

block components
  h2 Photos
  .photos
    each idx in Array.from(Array(25).keys())
        img.photos-img(src=`https://source.unsplash.com/random/${idx}`, alt="")